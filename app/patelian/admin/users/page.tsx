import { UsersList } from "@/components";
import React from "react";

const page = () => {
  return (
    <div className="h-screen bg-secondary w-full">
      <UsersList />
    </div>
  );
};

export default page;
